---
name: flyio
description: Look up Fly.io docs via context7 before deploying or modifying fly.toml
---

Use context7 with library ID `/superfly/fly-docs` for Fly.io docs.

This project deploys to Fly.io:
- Config: `fly.toml`
- Deploy: `flyctl deploy`
- Secrets: `flyctl secrets set KEY=value`
